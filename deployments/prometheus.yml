global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'canton-bridge-relayer'
    static_configs:
      - targets: ['relayer:9090']
        labels:
          service: 'relayer'
