server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "p@ssw0rd"
  database: "relayer"
  ssl_mode: "disable"

ethereum:
  rpc_url: "http://localhost:8545"  # Local Anvil for now
  ws_url: "ws://localhost:8545"
  chain_id: 31337
  bridge_contract: ""  # Set after deployment
  token_contract: ""   # Set after deployment
  relayer_private_key: "ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
  confirmation_blocks: 1
  gas_limit: 300000
  max_gas_price: "100000000000"
  polling_interval: "5s"
  start_block: 0

canton:
  # 5North DevNet Ledger API (gRPC over HTTPS)
  rpc_url: "canton-ledger-api-dev1.01.chainsafe.dev:443"
  ledger_id: ""  # Query from endpoint or leave empty
  domain_id: ""  # Set after connecting (get from 5N Dashboard)
  application_id: "canton-middleware"
  relayer_party: ""  # Allocate via 5N Dashboard
  bridge_package_id: ""  # After DAR upload
  core_package_id: ""
  bridge_module: "Wayfinder.Bridge"
  bridge_contract: "WayfinderBridgeConfig"
  
  # TLS Required (HTTPS endpoint)
  tls:
    enabled: true
  
  # Auth0 JWT Authentication
  # Auth0 URL: https://dev-2j3m40ajwym1zzaq.eu.auth0.com
  auth:
    token_file: "secrets/devnet-token.txt"
    # Or use OAuth2 client credentials:
    # auth0_url: "https://dev-2j3m40ajwym1zzaq.eu.auth0.com"
    # client_id: "${AUTH0_CLIENT_ID}"
    # client_secret: "${AUTH0_CLIENT_SECRET}"
    # audience: "https://canton-ledger-api-dev1.01.chainsafe.dev"
  
  polling_interval: "1s"

bridge:
  max_transfer_amount: "1000000000000000000000"
  min_transfer_amount: "1000000000000000"
  rate_limit_per_hour: 100
  max_retries: 3
  retry_delay: "10s"
  processing_interval: "5s"

monitoring:
  enabled: true
  metrics_port: 9090
  health_check_url: "/health"

logging:
  level: "debug"
  format: "console"
  output_path: "stdout"

