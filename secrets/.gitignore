# Ignore all files in this directory except .gitignore
*
!.gitignore

# This directory is for storing sensitive files like:
# - devnet-token.txt (Auth0 JWT token for DevNet)
# - mainnet-token.txt (Auth0 JWT token for MainNet)
# - Any other secrets

# Example: Save your JWT token here
# echo "your-jwt-token" > secrets/devnet-token.txt

